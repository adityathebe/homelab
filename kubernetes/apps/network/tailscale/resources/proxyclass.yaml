apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: homelab-default
spec:
  statefulSet:
    pod:
      tailscaleContainer:
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            memory: 256Mi
