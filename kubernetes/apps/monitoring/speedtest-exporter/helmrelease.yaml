---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: speedtest-exporter
  namespace: monitoring
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-helm-charts
        namespace: flux-system
  values:
    controllers:
      speedtest-exporter:
        containers:
          speedtest-exporter:
            image:
              repository: ghcr.io/adityathebe/speedtest-exporter
              tag: v1.2.1
            env:
              - name: LOG_LEVEL
                value: DEBUG
            resources:
              requests:
                cpu: 1m
                memory: 32Mi
              limits:
                memory: 64Mi
            securityContext:
              runAsUser: 1000
              runAsGroup: 1000
        defaultContainerOptionsStrategy: merge
        defaultContainerOptions:
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
    service:
      speedtest-exporter:
        controller: speedtest-exporter
        ports:
          http:
            port: 7777
    serviceMonitor:
      speedtest-exporter:
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 5m
            scrapeTimeout: 1m
