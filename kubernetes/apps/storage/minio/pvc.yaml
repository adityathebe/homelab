apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
spec:
  accessModes:
    - ReadWriteMany
    - ReadWriteOnce
  storageClassName: appdata
  resources:
    requests:
      storage: 25Gi
