apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gickup
spec:
  storageClassName: smb-backup
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
