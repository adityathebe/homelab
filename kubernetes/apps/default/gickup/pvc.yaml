---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gickup-storage
  annotations:
    nfs.io/storage-path: gickup
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: appdata
