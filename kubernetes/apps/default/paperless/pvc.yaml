---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-data
  annotations:
    nfs.io/storage-path: paperless
spec:
  storageClassName: kubedata
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 15Gi
