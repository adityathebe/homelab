---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: csi-driver-smb
  namespace: flux-system
spec:
  interval: 30m
  path: ./kubernetes/apps/network/csi-driver-smb
  targetNamespace: network
  sourceRef:
    kind: GitRepository
    name: flux-system
  prune: true
  wait: false
  decryption:
    provider: sops
    secretRef:
      name: sops-age
