apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mission-control
  namespace: music
spec:
  chart:
    spec:
      chart: mission-control
      version: 0.1.197-beta.5
      sourceRef:
        kind: HelmRepository
        name: flanksource
        namespace: music
      interval: 30m
  values:
    b: c
obj:
    a: 2